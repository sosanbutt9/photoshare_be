# Copy to .env if you want to override defaults from config/settings.py.
# Only SECRET_KEY is read from the environment; everything else is set in code.

SECRET_KEY=change-me-to-a-long-random-string-in-production
